/*const pool = require("../../config/database");

module.exports = {
	show: ([파라미터], callBack) => {
    		pool.query(`SQL 구문`,
      		(error, results, fields) => {
        		if (error) {
          			callBack(error);
        		}
        		return callBack(null, results);
      			}
    		);
  	},
	create: ([파라미터], callBack) => {
                pool.query(`SQL 구문`,
                (error, results, fields) => {
                        if (error) {
                                callBack(error);
                        }
                        return callBack(null, results);
                        }
                );
        },
	photoview: ([파라미터], callBack) => {
                pool.query(`SQL 구문`,
                (error, results, fields) => {
                        if (error) {
                                callBack(error);
                        }
                        return callBack(null, results);
                        }
                );
        },
}*/
